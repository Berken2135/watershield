services:
  - type: web
    name: watershield-backend
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    envVars:
      - key: PORT
        value: 8000
      - key: CORS_ORIGINS
        value: "*"
